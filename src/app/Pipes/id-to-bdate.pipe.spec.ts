import { IdToBdatePipe } from './id-to-bdate.pipe';

describe('IdToBdatePipe', () => {
  it('create an instance', () => {
    const pipe = new IdToBdatePipe();
    expect(pipe).toBeTruthy();
  });
});
